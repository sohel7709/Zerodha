DATABASE_URL=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/zerodha?retryWrites=true&w=majority
PORT=8080
# Set to "production" on Railway so dev-only seed endpoints are disabled
NODE_ENV=production
